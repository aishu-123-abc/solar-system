name: solar system workflow
on: 
    workflow_dispatch:
    push:
        branches:
          - 'feature/*'
          - main
env:
    MONGO_URI: 'mongodb+srv://supercluster.d83jj.mongodb.net/superData'
    MONGO_USERNAME: ${{ vars.MONGO_USERNAME }}
    MONGO_PASSWORD: ${{ secrets.MONGO_PASSWORD }}

jobs:
    unit-testing:
        name: unit testing
        strategy:
            matrix:
              os: [ubuntu-latest]
              nodejs_version: [18, 20]
#            max-parallel: 3
            fail-fast: false
        runs-on: ${{ matrix.os }}
        steps:
            - name: checkout repository
              uses: actions/checkout@v5

            - name: nodejs setup version ${{ matrix.nodejs_version }}
              uses: actions/setup-node@v5
              with:
                node-version: ${{ matrix.nodejs_version }}

            - name: Cache Dependencies
              uses: actions/cache@v4
              with:
                 path: node_modules
                 key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}
            
            - name: Installation npm
              run: npm install

            - name: Unit testing
              id: nodejs-unit-testing-step
              run: npm test

            - name: Archive test results
              if: always()
#                failure() || (steps.nodejs-unit-testing-step.outputs == 'failure' || steps.nodejs-unit-testing-step.outputs == 'success') 
              uses: actions/upload-artifact@v4
              with:
                name: Artifacts upload ${{ matrix.nodejs_version}}-${{ matrix.os }}
                path: test-results.xml
       
    code-coverage:
        name: code-coverage
        runs-on: ubuntu-latest
        steps:
            - name: checkout repository
              uses: actions/checkout@v5

            - name: nodejs setup version 18
              uses: actions/setup-node@v5

            - name: Cache Dependencies
              uses: actions/cache@v4
              with:
                 path: node_modules
                 key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}
            
            - name: Installation npm
              run: npm install

            - name: run code coverage
              continue-on-error: true
              run: npm run coverage

            
